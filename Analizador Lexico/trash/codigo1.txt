/* 

* A program to perform Euclid's algorithm to computer
* the greates common denominator (GCD) of a pair of integers.
*/
float a, b, c;
int asd[3];
float dsa[gft];
int gcd(int u, int v, int w, int w1)
{
	a=8+6+23;
	aa=6;
	if(v==0) return u;
	else return gcd(v,u-u/v*v);
	/* u-u/v*v == u mod v */
}

void main(float m1, float m2, float m3, int m4, int m5)
{
	int x; int y;
	x=input();y=input();
	output(gcd(x,y));
}